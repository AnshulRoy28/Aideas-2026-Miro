# Infrastructure Component

AWS Lambda functions for automated Knowledge Base synchronization.

## Contents

- `lambda/` - Lambda function for auto-sync

## Quick Start

```bash
cd infrastructure/lambda
chmod +x deploy.sh
./deploy.sh
```

See `lambda/LAMBDA_SETUP.md` for detailed instructions.
